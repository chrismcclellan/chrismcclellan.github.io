var _ = require('lodash');
var percent = require("percent-value");

module.exports = function canopy() {

    var $canopy = $('#canopy');
    var $objective = $('.objective', $canopy);

    // window.setTimeout(function() {
    //     $objective.addClass('flash');
    // }, 1200);
}

